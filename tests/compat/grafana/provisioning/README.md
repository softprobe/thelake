# Grafana datasource provisioning placeholders (Phase 4)

# prometheus.yaml / loki.yaml / tempo.yaml will point at thelake compat endpoints.
# Phase 0 intentionally leaves these empty so CI does not imply Grafana parity.
