# Compat fixtures

Signal-neutral OTel fixtures shared across Prometheus, Loki, Tempo, and Grafana
tests. Prefer builders in `tests/util/otlp.rs` and helpers under
`tests/compat/support/`.

Canonical docs: `docs/compat/matrix.md`.
