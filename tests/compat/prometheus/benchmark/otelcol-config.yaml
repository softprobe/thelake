# Host metrics → Softprobe OTLP/HTTP (Prom micro-benchmark).
# Pin image via docker-compose.yml.
#
# Prefer larger collector batches in production so flush-through ingest does not
# create one tiny Parquet file per export. See docs/perf/prometheus-query-findings.md
# Phase B. The churn overlay (otelcol-config.churn.yaml) deliberately does the opposite.

receivers:
  hostmetrics:
    collection_interval: 5s
    scrapers:
      cpu:
      memory:
      load:
      network:

processors:
  resource:
    attributes:
      - key: service.name
        value: prom-bench-host
        action: upsert

exporters:
  otlphttp/softprobe:
    endpoint: http://host.docker.internal:8090
    tls:
      insecure: true
    headers:
      Authorization: "Bearer local-dev-key"
  debug:
    verbosity: basic

service:
  pipelines:
    metrics:
      receivers: [hostmetrics]
      processors: [resource]
      exporters: [otlphttp/softprobe, debug]
