Ok, let's work on the code now. Remember one things. We have used a lot of effort to optize a few things, don't lose these features: 1) don't write too many small staged files in the local drive. 2) WAL should not mixed in other files so that we don't have to scan large directory if we need read stage files. 3) buffer as much as we can, don't stage too often. 4). Commit even less often. 5) Clean up WAL and staged files after we stage or commited writes. 6) pin metadata, don't read too often from catalog and cache manefest 